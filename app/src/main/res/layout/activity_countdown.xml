<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CountdownActivity"
    android:background="@color/colorTitleBackground">

    <ImageView
        android:id="@+id/back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="30dp"
        android:layout_marginStart="50dp"
        android:src="@drawable/chevron_left"/>

    <TextView
        android:id="@+id/count_down"
        android:layout_width="@dimen/count_down1"
        android:layout_height="@dimen/count_down1"
        android:layout_centerInParent="true"
        android:background="@drawable/count_down_bg"
        android:gravity="center"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:textSize="@dimen/count_down_text1"
        android:text="5"/>

    <TextView
        android:id="@+id/train_tips"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/count_down"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/count_down2"
        android:textColor="@android:color/black"
        android:textSize="@dimen/count_down_text2"
        android:text="@string/count_down_tips" />

</RelativeLayout>